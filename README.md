# herosmsactivate

Python wrapper for the HeroSMS API.

This project is a fork/adaptation of the `smsactivate` library, modified to work with the HeroSMS service after the shutdown of SMS-Activate.

## Features

- Same API structure as `smsactivate`
- Minimal changes (endpoint replacement)
- Drop-in replacement for existing projects
- Compatible with Python 3.10+